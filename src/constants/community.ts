/**
 * The community server. Linked from the sidebar and from the supporter page —
 * where it doubles as the fallback for a donation whose email never matched an
 * account, so it lives here rather than as a literal in whichever file needed
 * it first.
 */
export const DISCORD_INVITE_URL = "https://discord.com/invite/6yJmsZW2Ne";
