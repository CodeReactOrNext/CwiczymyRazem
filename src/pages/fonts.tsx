import { Inter, Teko } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const teko = Teko({
  subsets: ["latin"],
  variable: "--font-teko",
  display: "swap",
});
